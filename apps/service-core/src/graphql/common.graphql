extend schema
@link(
  url: "https://specs.apollo.dev/federation/v2.3"
  import: ["@key", "@shareable"]
)

type ApiResponse {
  ok: Boolean
  message: String
  details: [String]
}
type PaginationMeta {
  total: Int!
  page: Int!
  limit: Int!
  totalPages: Int!
}
