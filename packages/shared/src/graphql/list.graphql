type PaginationMeta {
  total: Int!
  page: Int!
  limit: Int!
  totalPages: Int!
}

type TeacherList {
  data: [Teacher!]!
  meta: PaginationMeta!
}

type StudentList {
  data: [Student!]!
  meta: PaginationMeta!
}

type ClassList {
  data: [Classe!]!
  meta: PaginationMeta!
}

type DeleteResponse {
  ok: Boolean!
  message: String!
}

