input CreateLessonInput {
    day: Day!
    mode: ResourceMode!
    startTime: DateTime!
    endTime: DateTime!
    subjectId: ID!
    teacherId: ID
    groupId: ID
}

input UpdateLessonInput {
    id: ID!
    startTime: DateTime
    endTime: DateTime
    groupId: ID
    teacherId: ID
    subjectId: ID
    day: Day
    mode: ResourceMode!
}
input CreateTeacherAssignmentInput {
    classId: ID!
    subjectIds: [ID!]!
    teacherId: ID!
}
